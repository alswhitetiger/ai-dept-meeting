# 이 앱은 Anthropic API 키가 필요 없습니다.
# Claude Agent SDK 가 Claude Code CLI 를 통해 당신의 Claude 구독 로그인으로 인증합니다.
# (준비: Node.js 설치 → `npm i -g @anthropic-ai/claude-code` → `claude` 실행 후 `/login` 으로 구독 로그인)

# 사용할 모델 별칭: "sonnet" | "opus" | "haiku"  (Max 구독이면 opus 권장)
MODEL=sonnet
